"use strict"; // Mode strict (slide 12)

// Fonction f1: Affiche le nom (slide 40 - fonctions)
function f1() {
    let nom = "John"; // Déclaration de variable (slide 15)
    console.log(nom); // Affichage console (slide 13)
}

// Fonction f2: Affiche le prénom (slide 40)
function f2() {
    let prenom = "Doe"; // Variable (slide 15)
    console.log(prenom); // Affichage (slide 13)
}

// Fonction f3: Affiche nom et prénom concaténés (slide 40)
function f3() {
    let prenom = "John"; // Variable (slide 15)
    let nom = "Doe"; // Variable (slide 15)
    let nomComplet = prenom + "-" + nom; // Concaténation (slide 17)
    console.log(nomComplet); // Affichage (slide 13)
}

// Fonction f4: Affiche l'âge (slide 40)
function f4() {
    let age = 30; // Variable de type number (slide 15)
    console.log(age); // Affichage (slide 13)
}

// Fonction f5: Affiche la note (slide 40)
function f5() {
    let note = 15; // Variable number (slide 15)
    console.log(note); // Affichage (slide 13)
}

// Fonction f6: Affiche la valeur de Pi (slide 40)
function f6() {
    let pi = 3.14; // Variable float (slide 15)
    console.log(pi); // Affichage (slide 13)
}
